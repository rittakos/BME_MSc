function result = system(OTF, I)



%ratio = size(I, 1) / size(OTF, 1);
%OTF = imresize(OTF, ratio);
%result = real(ifft2(fft2(I) .* OTF));

sourceSize = size(I, 2);
[X_samples,Y_samples] = meshgrid(linspace(1,sourceSize(2),targetSize(2)), linspace(1,sourceSize(1),targetSize(1)));
newI = interp2(sourceIm, X_samples, Y_samples);

result = newI .* OTF;

end